# ShellX 3.0.1 冻结与锁屏分析

依据本地 ScreenshotShell.arm64e 的带符号反汇编，不复制二进制实现。

- 0x1881c0–0x188400 注册控制中心、CoverSheet、锁屏及 grabber 的独立 hook。
- 0x188708/0x188778/0x1887f0/0x188884 在冻结时阻止控制中心展示。
- 0x188918/0x1889b0 在 CoverSheet 展示前清理覆盖层，再执行原方法。
- 0x188a50/0x188ae0 在 lockUIFromSource 两个入口清理覆盖层，再执行原锁屏方法。
- 0x188b94 处理 SBGrabberTongue 的手势入口。
- 0x105600 开启屏幕边缘手势延后；0xcd600 返回全部边缘。

本次移除 RegionShot 全局系统手势禁用断言，采用上述独立入口，保留已有边缘延后与触点过滤。对私有方法先检查运行时签名。锁屏清理同步执行，不等待动画或再次模拟电源键。静态分析不能代替 iOS 15.6 RootHide 实机验证，需同时测试两侧顶部拖动、锁屏及解锁后的再次截图。
