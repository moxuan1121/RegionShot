# 用户演示视频对应的实现目标

参考：用户提供的 `8d3aa065f2fb4249b2e8ba3f6d69ef90.mp4`，约 32.27 秒，592 × 1280。
这是实际交互验收参考，优先于早期文档中“工具条始终固定底部”的描述。

| 视频片段（约） | 可见行为 | 0.4.0 调整 |
| --- | --- | --- |
| 0–2 秒 | 进入压暗的冻结画面，初始五项工具条位于底部 | 初始工具条仅显示基础项，保留安全区间距 |
| 3–11 秒 | 拖出或调整矩形，选区内保持明亮；四角为 L 形；操作条跟随选区，拖动中隐藏 | L 形控制点、拖动期间隐藏操作条、松手恢复；优先放选区下方，空间不足放上方并限制在安全区域 |
| 12–16 秒 | 退出后再次通过状态栏附近的滑动进入 | 增加可开关的状态栏右半侧右滑入口；沿用用户提供的 statusbar-shot 右半侧手势范围 |
| 17–24 秒 | 选区转为浮图，保持原位置与比例；随后拖动、贴边和弹出原生长按菜单 | 普通区域截图浮图保持选区 frame；默认启用双击选区生成浮图；原生长按菜单可自定义 |
| 27–31 秒 | 再次进入初始截图工作区，并取消回到原界面 | 初始/已选区工具条根据状态切换，取消仍释放冻结图并恢复操作 |

视频中的触摸轨迹用于观察手势，本轮没有将其作为 RegionShot 的视觉效果。
具体确认手势和动画手感仍需真机对照；视频无法证明 OCR、AI、长图、完整编辑器或其他设置已经实现。

尚未完全一致：选区操作条的具体按钮集合（复制、保存、翻译等直达操作）、五类菜单的独立配置、全部背景/样式，以及浮图缩放/吸边的细节动画。
本轮不把“主要交互已调整”描述为完整一比一复刻。
